{
  "name": "payment-failure-recovery-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "test": "node --experimental-vm-modules node_modules/jest/bin/jest.js --runInBand",
    "test:watch": "node --experimental-vm-modules node_modules/jest/bin/jest.js --watch",
    "lint": "node --check src/app.js && node --check src/server.js",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "prisma:deploy": "prisma migrate deploy",
    "demo:showcase": "node scripts/showcase-recovery-batch.js",
    "demo:showcase:reset": "node scripts/showcase-recovery-batch.js --reset",
    "demo:showcase:cleanup": "node scripts/showcase-recovery-batch.js --cleanup",
    "demo:showcase:report": "node scripts/showcase-recovery-batch.js --report",
    "demo:showcase:verify": "node scripts/showcase-recovery-batch.js --verify"
  },
  "dependencies": {
    "@prisma/client": "^6.16.2",
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "jest": "^30.1.3",
    "prisma": "^6.16.2",
    "supertest": "^7.1.4"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
